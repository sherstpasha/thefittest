name: Code checks

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  test-base:
    name: Test without PyTorch
    runs-on: ubuntu-latest
    strategy: 
      matrix:
        python-version: ["3.8", "3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install base dependencies
        run: |
          python -m pip install --upgrade pip
          pip install "numpy>=1.23,<2.0" numba scipy "scikit-learn>=1.4" joblib==1.2.0
          pip install pytest pytest-cov

      - name: Run tests (NN tests will be skipped)
        run: |
          pytest -v


  test-full:
    name: Test with PyTorch
    runs-on: ubuntu-latest
    strategy: 
      matrix:
        python-version: ["3.8", "3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install PyTorch (CPU version for CI)
        run: |
          python -m pip install --upgrade pip
          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

      - name: Install thefittest and dev dependencies
        run: |
          pip install "numpy>=1.23,<2.0" numba scipy "scikit-learn>=1.4" joblib==1.2.0
          pip install pytest pytest-cov

      - name: Generate coverage report
        run: |
          pytest --cov=./ --cov-report=xml -v

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v3
